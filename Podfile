# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'realm_r_sample' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for realm_r_sample
  pod 'RealmSwift'
  pod 'R.swift'

  target 'realm_r_sampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'realm_r_sampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
